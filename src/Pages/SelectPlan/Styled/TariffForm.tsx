import styled from "styled-components";

const TariffForm = styled.div`
  width: 600px;
  height: 530px;
  background-color:${({theme}) => theme.box};
  border-radius: 10px;
`;

export default TariffForm;
